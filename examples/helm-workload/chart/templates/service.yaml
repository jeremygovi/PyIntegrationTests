apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    pyintegrationtests.io/run: {{ .Values.runId | quote }}
spec:
  selector:
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - {name: http, port: 80, targetPort: http}
